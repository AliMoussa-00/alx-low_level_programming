#include "main.h"

/**
* add - adds two integers and returns the result.
* @i: the number to add
* @j: the second number to add
*
* Return: value of the addition.
*/
int add(int i, int j)
{
	return (i + j);
}
